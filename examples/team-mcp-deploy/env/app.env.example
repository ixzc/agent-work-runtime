# Runtime service unit only — restricted application role (no BYPASSRLS).
# Never the schema owner. Prefer private-net PG; do not default to public PG.
AWR_TEAM_DATABASE_URL=postgres://awr_app:replace-me@127.0.0.1:5432/awr_team?sslmode=require
